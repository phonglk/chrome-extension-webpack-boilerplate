import React from 'react';
import { render } from 'react-dom';

const OptionPage = () => (
  <div>Just Another Chrome Extension Option Page</div>
);

render(<OptionPage />, document.getElementById('root'));
